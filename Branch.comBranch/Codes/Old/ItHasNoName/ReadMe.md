
###关于代码：

- 直接由官方库所给的模板修改而来，所以有很多注释，实际代码不多。

- 虽然在System\system_stm32f4xx.c中已经有各种初始化代码了，并且会在main函数之前执行，但是为了方便，还是重写各种初始化代码于Init.c中。

- main.c中有默认的SysTick初始化，已经被移植至Init.c中，目前只用作延时函数，不过可以在中断中加入别的功能。

- 请保证中断处理函数的高效性。

- 请尽量避免任何类型的warnings，目前编译所出的warnings是由没有初始化GPIO和SPI造成。


###移植进度：

- 所有代码均为理论，尚未调试；

- Init.c和SPI.c中的初始化代码均不完整，所有实际的初始化代码均被注释，根据实际接线方式决定使用何种初始化代码（也由此造成了几个warnings）；

- DW1000中的初始化函数106行的设置ISR为上升沿触发回调函数由于外部中断尚未设置故没有调用。就目前情况来看，初始化时其实是setCallbacks(NULL, NULL)的，所以可以暂时不考虑；

- 如果需要加中断时，考虑使用函数指针实现回调函数的切换(当然DW1000的这段代码里面已经可以确定是ISR()了，具体调用的函数由ISR()选择)
